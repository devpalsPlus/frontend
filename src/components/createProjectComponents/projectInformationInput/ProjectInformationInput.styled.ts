import styled from 'styled-components';

export const InfoRow = styled.div`
  align-items: center;
  gap: 10px;
  margin-bottom: 13px;
  display: flex;

  label {
    font-size: 0.8rem;
    font-weight: bold;
    color: #333;
    flex: 0.11;
  }

  p {
    font-size: 0.8rem;
  }
`;
