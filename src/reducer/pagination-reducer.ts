export default function paginationReducer(paginationArr, action) {
  switch (action.type) {
    case 'doubleLeft': {
    }
    case 'left': {
    }
    case 'right': {
    }
    case 'doubleRight': {
    }
  }
}
